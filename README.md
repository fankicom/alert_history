**Alert History**<br>
Visualization of history, pending and configuration from the app *alarm_send* in Home Assistant.<br>
Three state arrays are created in Home Assistant, which can be displayed in a readable form using normal cards (e.g., custom: button-card).
